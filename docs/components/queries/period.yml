StartQuery:
  in: query
  name: start
  description: "Start of the period"
  required: false
  schema:
    type: string
    format: date-time
EndQuery:
  in: query
  name: end
  description: "End of the period"
  required: false
  schema:
    type: string
    format: date-time
