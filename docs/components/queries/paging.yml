PageQuery:
  in: query
  name: page
  description: "Page number"
  required: false
  schema:
    type: number
    default: 1
SizeQuery:
  in: query
  name: size
  description: "Amount of elements per page"
  required: false
  schema:
    type: number
    default: 20
