{
    "auth": {
        "token_key": "${DIABETIPS_AUTH_TOKEN_KEY}"
    },
    "db": {
        "type": "mariadb",
        "url": "${DIABETIPS_DB_URL}"
    },
    "mail": {
        "url": "${DIABETIPS_MAIL_URL}"
    }
}
